# 🚀 立即创建管理员账号 - 3步完成

## 第一步：打开 Supabase

1. 访问 https://supabase.com/dashboard
2. 登录你的账号
3. 选择你的项目（AI-Counselor 项目）

---

## 第二步：打开 SQL 编辑器

1. 在左侧菜单中，点击 **SQL Editor**（或者图标 `</>`）
2. 点击右上角的 **New Query** 按钮

---

## 第三步：执行 SQL 脚本

### 复制以下完整 SQL 代码：

```sql
-- 启用加密扩展
CREATE EXTENSION IF NOT EXISTS pgcrypto;

-- 确保表存在
CREATE TABLE IF NOT EXISTS public.admin_users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  username TEXT UNIQUE NOT NULL,
  password_hash TEXT NOT NULL,
  email TEXT UNIQUE NOT NULL,
  role TEXT NOT NULL DEFAULT 'admin',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 创建管理员账号
INSERT INTO public.admin_users (username, email, password_hash, role)
VALUES (
  'admin_secure_2025',
  'admin@ai-counselor.com',
  crypt('P@ssw0rd!Sec7ure#2025', gen_salt('bf', 10)),
  'admin'
)
ON CONFLICT (username) 
DO UPDATE SET 
  password_hash = crypt('P@ssw0rd!Sec7ure#2025', gen_salt('bf', 10)),
  updated_at = NOW();

-- 验证
SELECT username, email, role, created_at 
FROM public.admin_users 
WHERE username = 'admin_secure_2025';
```

### 执行步骤：

1. 将上面的 SQL 代码粘贴到 SQL 编辑器中
2. 点击右下角的 **Run** 按钮（或按 `Ctrl+Enter`）
3. 等待几秒钟，看到成功消息

---

## ✅ 验证成功

你应该在结果中看到：

```
username: admin_secure_2025
email: admin@ai-counselor.com
role: admin
created_at: [当前时间]
```

---

## 🎉 开始使用

现在访问后台登录页面：

**登录地址：** 
```
https://your-domain.vercel.app/ad7m2in9/login
```

**登录凭据：**
- **用户名：** `admin_secure_2025`
- **密码：** `P@ssw0rd!Sec7ure#2025`

---

## ⚠️ 如果遇到问题

### 问题1：提示 "relation admin_users does not exist"
**解决方案：** 先运行 `scripts/001_create_tables.sql`

### 问题2：提示 "extension pgcrypto does not exist"
**解决方案：** 在 SQL 编辑器中先运行：
```sql
CREATE EXTENSION IF NOT EXISTS pgcrypto;
```

### 问题3：密码无法登录
**解决方案：** 重新运行上面的 SQL（使用 ON CONFLICT 会自动更新密码）

---

**完成后刷新登录页面，输入凭据即可登录！** 🎊

